


class CrawlZingNews:
    pass
        # if cls.checkTheArticleIsExist(connection, site_loc):
        #     return False
        # image = url.find('sitemap_image:image', ns)
        # # image url
        # image_url = image.find('sitemap_image:loc', ns).text
        # # title
        # title = image.find('sitemap_image:caption', ns).text
        # # publish_date
        # publish_date = url.find('sitemap:lastmod', ns).text.replace('T', ' ').replace('+07:00', ':00')
        # return [title, 'Author', 0, 1, image_url, 'ZingNews', publish_date, publish_date, publish_date]


